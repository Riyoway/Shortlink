{
  "name": "personal-shortlink",
  "version": "0.1.0",
  "private": false,
  "description": "A tiny self-hosted shortlink service for your own domain.",
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "shortlink",
    "url-shortener",
    "cloudflare-workers",
    "self-hosted"
  ],
  "license": "MIT",
  "dependencies": {
    "@upstash/redis": "^1.38.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.8.3"
  }
}
